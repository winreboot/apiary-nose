name: Validate sessions
on:
  pull_request:
    paths: ["data/**", "schema/**", "tools/**"]
  push:
    branches: [main]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install jsonschema
      - name: Validate every session file
        run: python tools/validate.py 'data/*/sessions/*.json'
